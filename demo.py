import dummyprinter, string, turtle
d = dummyprinter.DummyPrinter()
#d.write(string.ascii_lowercase, 10)
turtle.home()
raw_input("press enter to start")
d.write("i think this looks stylish, but the nxt plotter on paper looks crap", 10)
raw_input("press enter to quit")
